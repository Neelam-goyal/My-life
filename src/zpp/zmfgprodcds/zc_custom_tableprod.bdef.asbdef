projection;
strict ( 2 );
use draft;

define behavior for ZC_CUSTOM_TABLEPROD
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
}